import { Request, Response } from "express";
import config from "../../../infraestructure/config/config";

export const approvetIndividial = async (
  req: Request,
  res: Response
): Promise<void> => {

/**
 * debo recepcionar los datos
 * el usuario me debe enviar 
 * tipodocumento
 * documento 
 * 
 * 
*/

};


/** 
 * 
 * en la authenticacion deberia manejar 
 * de que sisitema pertenece 
 * que usuario accedi
 * 
 * inica secion 
 * el sisitema me envia me deveria enviar su cliendId y el tokenID 
 * el usuario me envia el Token de user , para ello debo ver como authenticar el usuario con el token del sistema que se conecto , asi yo gestiono los usuarios
 * el usuario ahora me enviaria el token de 
 */